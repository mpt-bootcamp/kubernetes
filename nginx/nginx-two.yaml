apiVersion: v1
kind: Service
metadata:
  name: nginx-two
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: ngnix-two

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-two
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-two
  template:
    metadata:
      labels:
        app: nginx-two
    spec:
      containers:
      - name: nginx-one
        image: mptbootcamp/ngnix:latest
        ports:
        - containerPort: 8080
        env:
        - name: MESSAGE
          value: Hello from the second deployment!

